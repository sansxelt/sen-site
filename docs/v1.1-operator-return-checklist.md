# V1.1 operator return checklist (founder AFK window, 2026-07-12)

Standing rule this window: migrations 7 and 8 stay UNAPPLIED. S3/S4 code is committed + pushed but their
Vercel deployment is DEFERRED until each migration is applied in order. New code fails clearly (never
silently degrades) when its migration is missing. Railway auto-builds from main on push; only worker-safe
changes are pushed (S5 boundaries are designed backward-compatible; see its commit message).

## On return, in this exact order
1. Read the latest "AFK window report" section at the bottom of docs/v1-release-progress.md.
2. Apply sql/vraelis-preflight-7-context-snapshots.sql in Supabase.
3. Say "migration 7 applied" - I verify schema + owner isolation live, then deploy the S3 build to Vercel.
4. Apply sql/vraelis-preflight-8-deployments.sql.
5. Say "migration 8 applied" - I verify, then deploy the S4 build.
6. Review anything the window's code-review or security passes flagged as needing a human call.

## What was allowed to deploy during the window
- S2 connection management (no new migrations required) - deployed after its verification battery.
- Nothing that requires migration 7 or 8.

## What was NOT done while AFK (stop-gates honored)
- No migrations applied, no Stripe changes, no new providers, no GitHub App registration, no public
  availability changes, no destructive anything.
