// Shown by Next.js while a (site) page's server component resolves.
// Returns null — the body's bg-background (#0A0F18) fills the screen,
// preventing any white/blank flash during navigation.
export default function SiteLoading() {
  return null;
}
