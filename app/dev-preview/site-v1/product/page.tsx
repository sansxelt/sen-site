import Product from "./product";

// Server wrapper for the Product page. The view is a client component: it carries the reveal system and the
// progressively-assembling Guarantee signature.
export default function Page() {
  return <Product />;
}
