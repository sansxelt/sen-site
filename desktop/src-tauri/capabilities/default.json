{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — applies to splash + main windows",
  "windows": ["main", "splash"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
